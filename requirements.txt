# MLB betting pipeline dependencies (pinned to the versions verified locally).
# Used by the GitHub Actions cloud pulls and reproducible local installs.

# Data pulls / HTTP
requests==2.34.2

# LLM reasoning (Coach Bo)
anthropic==0.121.0

# Database backends
psycopg2-binary==2.9.12     # Postgres (Neon) driver
SQLAlchemy==2.0.51          # available for the abstraction layer (SQLite is stdlib)

# Config
python-dotenv==1.2.2
pytz==2026.2

# Analysis / dashboard (pandas also used by pull_tendencies_nfl.py for pbp aggregation)
pandas==3.0.3
numpy==2.4.6
streamlit==1.58.0
plotly==6.7.0
altair==6.1.0
